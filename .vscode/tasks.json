{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "${env:GODOT_BIN}",
            "type": "process",
            "args": [
                "--build-solutions",
                "--path",
                "${workspaceRoot}",
                "--no-window",
                "-q"
            ],
            "problemMatcher": "$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}